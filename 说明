本插件在以下环境运行正常：

  机器：MacbookAir
  浏览器环境：chrome58.0.3029.110 (64-bit) for mac
  服务器：Apache/2.4.23 (Unix)

操作方法：

    0、 将chartsTested文件置于服务器目录下，打开charts.html,需选择的json文件位于
        chartsTested/json/;

    1、数据类型分为两类:

      pieData即饼状图数据;otherData即折线、柱状图数据

      选择完数据文件后，自动刷新出表格
      由此：当刷新出表格后，

      若为otherData,通过点击 第一行 和 第一列 的若干单元格，实现数据筛选，点击”生成图表”，生成综合图表。
      若为pieData,通过点击第一行的若干单元格，实现数据筛选，点击“生成图表”，生成饼状图

    2、可以再次进行数据筛选，再次点击“生成图表”，更新图表。

    3、在生成的图表中，若为综合图表，右上角的6个图标可实现图表类型的切换和图表的下载。

注意事项：

     0、在进行数据筛选时，选中的otherData的行数据数量限制默认>=2,列数据数量无限制
     1、当浏览器窗口大小变化时，可能由于css布局原因造成布局错乱。
